import styled from "styled-components";

export const OurNumbersWrap = styled.div`
  display: flex;
  margin: 8vh 0vw 0vh 0vw;
`;

export const LeftImage = styled.div``;

export const BackgroundImage = styled.img`
  width: clamp(10rem, 17vw, 50rem);
`;

export const BothContainer = styled.div`
  position: relative;
  transform: rotate(-90deg);
  top: clamp(5.5rem, 8.2vw, 40rem);
  left: clamp(10.5rem, 17.2vw, 20rem);
  width: 12vw;
`;

export const Heading = styled.div`
  font-weight: 500;
  font-size: 1.2vw;
  line-height: 1.95vw;
  color: #828282;
`;

export const NumbersContent = styled.div`
  font-weight: 700;
  font-size: 3.4vw;
  line-height: 4.49vw;
  color: #1d1d1d;
  width: 23vw;
  border-bottom: 0.8vw solid #f9c51c;
  height: 3.77vw;
`;

export const OurHeadContainer = styled.div``;

export const OurHead = styled.p`
  margin: 13.2vw 0vw 0vh 0vw;
  width: clamp(10rem, 62vw, 60rem);
`;

export const SpanHeading = styled.span`
  color: black;
  font-weight: 700;
  font-size: clamp(0.5rem, 1.5vw, 2.5rem);
`;

export const Description = styled.p`
  margin-top: 2vw;
  font-size: clamp(0.6rem, 1.35vw, 15rem);
`;

export const BorderBlank = styled.div`
  border-bottom: 3px solid #1d2e88;
  width: 60.5vw;
  margin: 2.8vw 0vw 0vh 0vw;
`;

export const TeamWrap = styled.div`
  display: flex;
  margin: 2.8vw 0vw 0vh 0vw;
  justify-content: space-between;
`;

export const CodeWrap = styled.div`
  color: #1d2e88;
`;

export const Code = styled.div`
  align-items: center;
  font-weight: 700;
  font-size: clamp(1rem, 4vw, 5rem);
`;

export const CodeDescription = styled.p`
  font-weight: 500;

  font-size: clamp(0.6rem, 2vw, 3rem);
`;

export const BorderRight = styled.div`
  border-right: 1px solid #1d2e88;
  height: 4.5vw;
  margin: 2vh 0vw 0vh 0vw;
`;
export const MembersWrap = styled.div`
  color: #1d2e88;
`;

export const Members = styled.div`
  align-items: center;
  font-weight: 700;
  font-size: clamp(1rem, 4vw, 5rem);
`;

export const MembersDescription = styled.p`
  font-weight: 500;

  font-size: clamp(0.6rem, 2vw, 3rem);
`;

export const BorderRightBlank = styled.div`
  border-right: 1px solid #1d2e88;
  height: 10vh;
  margin: 2vh 0vw 0vh 0vw;
`;

export const ProjectWrap = styled.div`
  color: #1d2e88;
`;

export const Projects = styled.div`
  align-items: center;
  font-weight: 700;
  font-size: clamp(1rem, 4vw, 5rem);
`;

export const ProjectDescription = styled.p`
  font-weight: 500;
  font-size: clamp(0.6rem, 2vw, 3rem);
`;
